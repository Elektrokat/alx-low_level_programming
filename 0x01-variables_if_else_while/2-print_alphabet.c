#include <stdio.h>

/**
 * main - entry poiht of code
 *
 * Return: always 0 if successful
 */
int main(void)
{
	char y;

	for (y = 'a'; y <= 'z'; y++)
	{
	putchar(y);
	}
	putchar('\n');

	return (0);
}
